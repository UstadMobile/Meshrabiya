package com.ustadmobile.meshrabiya.vnet

/**
 * Simple marker file for future factory pattern implementation if needed.
 * Currently, the tests use proper Robolectric configuration instead of dependency injection.
 */
object AndroidVirtualNodeFactory {
    // Future factory methods can be added here if needed
}
